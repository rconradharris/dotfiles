startup_message off
defscrollback 5000
vbell off
caption always "%{= kw}%-w%{= BW}%n %t%{-}%+w %-= ${USER}@%H - %LD %d %LM - %c"

# This will create each new window as a login shell so that .bash_profile is
# sourced (the default will be a non-login shell where .bashrc is sourced)
shell -$SHELL
